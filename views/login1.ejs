<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <!-- Compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.2/css/all.min.css" 
    integrity="sha512-1sCRPdkRXhBV2PBLUdRb4tMg1w2YPf37qatUFeS7zlBy7jJI8Lf4VHwWfZZfpXtYSLy85pkm9GaYVYMfw5BC1A=="
     crossorigin="anonymous" referrerpolicy="no-referrer" />
     <link rel="stylesheet" href="css/style.css">
    <title>Document</title>
</head>
<body>
    <section class="login-container">
        <i></i>
        <p class="lead"><%= title %>  in to your Application</p>
        <a class="btn red-darken-1" href="/auth/google">
            <i class="fab fa-google left"></i>Login with Google</a>

       
            <% if (locals.messages.errors) { %>
              <% messages.errors.forEach( el => { %>
                  <div class="alert alert-danger"><%= el.msg %></div>
              <% }) %>    
          <% } %>
            <form  method="POST" action="/login">   
                <h1>Login with  Email</h1>
                <div class="form-group row">
                    <label for="email" class="col-sm-2 col-form-label">Email</label>
                    <div class="col-sm-10">
                      <input type="email" class="form-control" name="email" id="email" placeholder="temporarily unavailable">
                    </div>
                  </div>
                <div class="form-group row">
                  <label for="password" class="col-sm-2 col-form-label">Password</label>
                  <div class="col-sm-10">
                    <input type="password" class="form-control" name="password" id="password" placeholder="Password">
                  </div>
                </div>
                <input class="btn btn-primary" type="submit" value="Login" >
              </form>
              <span>Don't have an account ? <a href="/signup">Sign up</a></span>
    </section>

          <!-- Compiled and minified JavaScript -->
          <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
          <!-- <script type="text/javascript" src="js/app.js"></script> -->
</body>
</html>



<div class="mt-4 page-heading">
  <div id="chatMessage-div">
    <% for(let i = 0; i< dataResponse.length; i++){%>
    <div class="parent-div <%- dataResponse[i].sender == userProfile._id ? 'own' : 'message'%>">
      <p id="messages"><%=dataResponse[i].text %></p>
      <span id="dateCreated"><%=format(dataResponse[i].createdAt)%></span>
    </div>
    <div id="conversationId" data-id="<%=dataResponse[i].conversationId%>"></div>
    <div id="senderId" data-id="<%=dataResponse[i].sender%>"></div>
    <div id="userId" data-id="<%=userProfile._id%>"></div>
    <% } %>
 </div>  
 <div id="form-div">
    <textarea id="text" placeholder="enter your message..."></textarea>   
    <button type="submit" id="sendId">Send</button>
 </div> 
</div> 